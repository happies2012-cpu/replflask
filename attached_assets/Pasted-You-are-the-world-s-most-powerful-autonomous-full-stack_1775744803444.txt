You are the world's most powerful autonomous full-stack AI development team with unlimited parallel execution capabilities.

**MISSION:** Build **EVERYTHING** I have previously requested — all pages, frontend, backend, workflows, integrations, chatbots, forms, auth/login, roles & permissions, and every single system — into one complete, production-ready web application.

**STRICT EXECUTION RULES (follow exactly, no exceptions):**
- Execute ALL tasks **in parallel** immediately.
- Automatically break every feature into intelligent subtasks.
- Automatically detect and resolve all dependencies on the fly.
- Prioritize **maximum speed** over any sequential order.
- If any information is missing, **infer it** or create sensible, professional default values instantly.
- If any subtask fails, **retry 3 times** with smart fallback logic before moving on.
- **NEVER stop** until the entire application is 100% complete end-to-end.
- After completion, return a clean **final result summary** with folder structure, deployment instructions, and how to run locally + deploy to production.

**NON-NEGOTIABLE PRODUCTION REQUIREMENTS:**
- Fully responsive, pixel-perfect modern UI
- **Dark + Light mode** (auto-detect + manual toggle) with smooth transitions
- Latest 2026 UI animations & micro-interactions (Framer Motion or equivalent)
- **Glassy / glassmorphism** background effects with blur, transparency, and depth
- Lazy loading everywhere (images, components, routes, heavy sections)
- Stunning landing page(s) with **hero section + beautiful sliders/carousels** (smooth, parallax, auto-play, mobile-optimized)
- Professional, premium feel throughout

**Tech Stack (use latest stable versions):**
- Frontend: Next.js 15+ App Router, TypeScript, Tailwind CSS v4, shadcn/ui + Radix primitives, Framer Motion, lucide-react
- Styling: Tailwind + glassmorphism utilities + custom CSS variables for dark/light
- Backend: Next.js Server Actions + API routes (or tRPC if needed), Prisma + PostgreSQL (Supabase/Neon)
- Auth: Clerk or NextAuth v5 with full **Roles & Permissions (RBAC)** system
- Forms: React Hook Form + Zod validation
- Chatbots: Vercel AI SDK + Grok/OpenAI compatible streaming chatbot (with memory & context)
- Workflows: Built-in automation engine + background jobs (use Upstash/Trigger.dev style)
- Database: Prisma ORM with proper relations, migrations, and seed data
- Other: TanStack Query, Zod, Sonner toast, next-themes, etc.

Generate the **complete project** with proper folder structure, all files ready to copy-paste, environment variables explained, Prisma schema, seed data, and full README with:
1. How to run locally
2. How to deploy to Vercel
3. How to set up the database
4. How to test all features (auth, roles, chatbot, workflows, forms, etc.)

Begin now. Output progress only if necessary, but aim to deliver the full working codebase as fast as possible. When finished, say **"✅ FULL PRODUCTION APPLICATION COMPLETE"** and provide the final summary + zip-ready structure.

Start building immediately.